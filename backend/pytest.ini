[pytest]
testpaths = tests
pythonpath = .
markers =
    unit: testes isolados (funções puras, sem HTTP)
    integration: testes com app Flask e/ou SQLite em arquivo temporário
    data: validação de JSONs de eventos
    scenario: simulações de partidas longas
