# Diálogo e roteiro

O protótipo com cenas em `src/scenes/*.js` foi **removido**. Toda narrativa de produção fica em **`backend/data/events/*.json`** (descrições, opções, condições e consequências).

Veja o schema em [EVENT_FORMAT.md](./EVENT_FORMAT.md) e exemplos nos arquivos em `backend/data/events/`.
